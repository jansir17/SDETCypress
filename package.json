{
  "name": "sdetcypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --headed --browser chrome",
    "cy:run:allure": "cypress run --headed --browser chrome --env allure=true",
    "cy:parallel": "cypress-parallel -s cy:run:allure -t 4 -d 'cypress/e2e' -a false"
  },
  "author": "mehrab",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.8.1"
  },
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "@shelex/cypress-allure-plugin": "^2.37.0"
  }
}
